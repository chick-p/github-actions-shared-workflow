name: Run dependabot automerge

on:
  pull_request:
    types:
      - opened
      - reopened

permissions: {}

jobs:
  call:
    permissions:
      contents: write
      pull-requests: write
    uses: chick-p/github-actions-shared-workflow/.github/workflows/dependabot-automerge.yml@main
